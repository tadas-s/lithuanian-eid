[Unit]
Description=Lithuanian eID signing server
StopWhenUnneeded=yes
BindsTo=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/lteid_server

[Install]
WantedBy=graphical-session.target
